# Rclone config for Dropbox. No secrets here — token comes from .env (RCLONE_DROPBOX_TOKEN).
# With RCLONE_CONFIG=./rclone-dropbox.conf (from .env), rclone uses ONLY this file;
# the user's global config (~/.config/rclone/rclone.conf) is never read or written.

[dropbox]
type = dropbox
